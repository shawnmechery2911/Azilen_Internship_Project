# Copy to .env only if you are using a local environment loader.
# Never commit real credentials.
AWS_REGION=us-east-2
AWS_BEARER_TOKEN_BEDROCK=replace-with-your-local-token
BEDROCK_MODEL_ID=us.anthropic.claude-...
